#include<stdio.h>
int fib(int n){
  if (n <= 1) {
    return n;
  }
  return fib(n-1) + fib(n-2);
}
int main(void){
  int value;
  printf("Enter the value: ");
  scanf("%d", &value);
  printf("\nValue of %d in fibonacci series is %d",value, fib(value));
  return 0;
}
